# Render Blueprint — https://render.com/docs/blueprint-spec
# One Node web service (React app + REST + WebSocket + CTO orchestrator) plus a
# managed Postgres (Basic tier, paid — persistent, no free-tier expiry), both in
# Singapore so the internal DB connection works.
# Our tables live in a dedicated `mission_control` schema.
databases:
  - name: mission-control-db
    plan: basic-256mb
    region: singapore

services:
  - type: web
    name: mission-control
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /login
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_VERSION
        value: 22.22.3
      - key: DATABASE_URL
        fromDatabase:
          name: mission-control-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: AUTH_USERNAME
        sync: false
      - key: AUTH_PASSWORD
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      # Flash has a free tier; gemini-2.5-pro is paid-only (needs billing).
      - key: GEMINI_MODEL
        value: gemini-2.5-flash
